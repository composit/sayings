<%= link_to "new entry", new_entry_path if( can? :create, Entry ) %>

<% for entry in @entries %>
  <div class="entry">
    <p><%= entry.content %></p>
    <div class="entry-footer">
      <%= entry.user.login %> - <%= entry.created_at.strftime( "%m.%d.%Y" ) %>
    </div>
  </div>
<% end %>
